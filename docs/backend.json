{
  "entities": {
    "Post": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Post",
      "type": "object",
      "description": "Represents a social media post, encompassing content, scheduling, and channel-specific configurations.",
      "properties": {
        "workspaceId": {
          "type": "string",
          "description": "The agency/client owning the post."
        },
        "createdByUid": {
          "type": "string",
          "description": "User ID of the user who created the post."
        },
        "source": {
          "type": "string",
          "enum": ["manual", "ai"],
          "description": "Source of the content."
        },
        "status": {
          "type": "string",
          "enum": ["draft", "scheduled", "publishing", "published", "failed"],
          "description": "Status of the post."
        },
        "title": {
          "type": "string",
          "description": "Title of the post."
        },
        "caption": {
          "type": "string",
          "description": "Final caption of the post."
        },
        "hashtags": {
          "type": "array",
          "description": "Array of hashtags associated with the post.",
          "items": {
            "type": "string"
          }
        },
        "callToAction": {
          "type": "string",
          "description": "Call to action text for the post."
        },
        "aiConfig": {
          "type": "object",
          "description": "Configuration details for AI-generated content (if applicable).",
          "properties": {
            "textPrompt": { "type": "string" },
            "imagePrompt": { "type": "string" },
            "model": { "type": "string" },
            "temperature": { "type": "number" }
          }
        },
        "media": {
          "type": "object",
          "description": "Information about the media associated with the post.",
          "properties": {
            "type": { "type": "string", "enum": ["image", "video", "null"] },
            "source": { "type": "string", "enum": ["upload", "ai", "external", "null"] },
            "storagePath": { "type": "string" },
            "generated": { "type": "boolean" },
            "width": { "type": "number" },
            "height": { "type": "number" },
            "aspectRatio": { "type": "string" }
          }
        },
        "channels": {
          "type": "array",
          "description": "Array of channels where the post will be published.",
          "items": {
            "type": "object",
            "properties": {
              "provider": { "type": "string", "enum": ["instagram", "facebook", "whatsapp"] },
              "accountId": { "type": "string" },
              "mode": { "type": "string" },
              "status": { "type": "string", "enum": ["draft", "scheduled", "publishing", "published", "failed"] },
              "scheduledAt": { "type": "string", "format": "date-time" },
              "publishedAt": { "type": "string", "format": "date-time" },
              "externalPostId": { "type": "string" },
              "lastError": { "type": "string" },
              "retryCount": { "type": "number" },
              "lastAttemptAt": { "type": "string", "format": "date-time" }
            }
          }
        },
        "metrics": {
          "type": "object",
          "description": "Metrics for the post (impressions, likes, comments, etc.).",
           "properties": {
            "impressions": { "type": "number" },
            "likes": { "type": "number" },
            "comments": { "type": "number" },
            "shares": { "type": "number" },
            "clicks": { "type": "number" }
          }
        },
        "nextRunAt": {
          "type": "string",
          "description": "The next scheduled time for this post to be processed by a job.",
          "format": "date-time"
        },
        "metricsLastUpdatedAt": {
          "type": "string",
          "description": "Timestamp of the last time metrics were updated for this post.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp of when the post was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of when the post was last updated.",
          "format": "date-time"
        },
        "deletedAt": {
          "type": "string",
          "description": "Timestamp of when the post was deleted (if applicable).",
          "format": "date-time"
        }
      },
      "required": [
        "workspaceId",
        "createdByUid",
        "source",
        "status",
        "caption",
        "channels",
        "createdAt",
        "updatedAt"
      ]
    },
    "Workspace": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Workspace",
      "type": "object",
      "description": "Represents a workspace (agency/client) in the system, enabling multi-tenant support.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the workspace."
        },
        "plan": {
          "type": "string",
          "enum": ["basic", "pro", "expert"],
          "description": "Subscription plan of the workspace."
        },
        "ownerUid": {
          "type": "string",
          "description": "User ID of the workspace owner."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp of when the workspace was created.",
          "format": "date-time"
        },
        "billingCustomerId": {
          "type": "string",
          "description": "Reference to billing information stored in the payment provider (e.g., Stripe)."
        }
      },
      "required": [
        "name",
        "plan",
        "ownerUid",
        "createdAt"
      ]
    },
    "WorkspaceUser": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "WorkspaceUser",
      "type": "object",
      "description": "Represents a user's membership in a workspace, defining their role and access level.",
      "properties": {
        "workspaceId": {
          "type": "string",
          "description": "ID of the workspace the user is a member of."
        },
        "uid": {
          "type": "string",
          "description": "User ID of the member."
        },
        "role": {
          "type": "string",
          "enum": ["owner", "admin", "member"],
          "description": "Role of the user in the workspace."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp of when the user joined the workspace.",
          "format": "date-time"
        }
      },
      "required": [
        "workspaceId",
        "uid",
        "role",
        "createdAt"
      ]
    },
    "SocialAccount": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SocialAccount",
      "type": "object",
      "description": "Represents a social media account connected to the system, storing display information but NOT sensitive tokens.",
      "properties": {
        "workspaceId": {
          "type": "string",
          "description": "ID of the workspace the social account belongs to."
        },
        "provider": {
          "type": "string",
          "enum": ["instagram", "facebook", "whatsapp"],
          "description": "Social media provider."
        },
        "displayName": {
          "type": "string",
          "description": "Display name of the social account."
        },
        "username": {
          "type": "string",
          "description": "Username of the social account."
        },
        "avatarUrl": {
          "type": "string",
          "description": "URL of the social account's avatar (profile picture).",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp of when the social account was connected.",
          "format": "date-time"
        },
        "connected": {
          "type": "boolean",
          "description": "Indicates whether the social account is currently connected."
        }
      },
      "required": [
        "workspaceId",
        "provider",
        "displayName",
        "username",
        "createdAt",
        "connected"
      ]
    },
    "Notification": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Notification",
      "type": "object",
      "description": "Represents a system notification for a user within a workspace.",
      "properties": {
        "workspaceId": {
          "type": "string",
          "description": "The workspace this notification belongs to."
        },
        "type": {
          "type": "string",
          "enum": ["post_published", "post_publish_failed", "metrics_updated"],
          "description": "The type of the notification."
        },
        "postId": {
          "type": "string",
          "description": "The ID of the related post, if applicable."
        },
        "channels": {
          "type": "array",
          "description": "The social media channels related to this notification.",
          "items": { "type": "string" }
        },
        "message": {
          "type": "string",
          "description": "The content of the notification message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the notification was created."
        },
        "readBy": {
          "type": "array",
          "description": "Array of user UIDs who have read the notification.",
          "items": { "type": "string" }
        }
      },
      "required": [ "workspaceId", "type", "message", "createdAt" ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "/workspaces/{workspaceId}": {
      "schema": { "$ref": "#/entities/Workspace" },
      "description": "Stores workspace information. Access is restricted to members of the workspace."
    },
    "/workspaceUsers/{membershipId}": {
       "schema": { "$ref": "#/entities/WorkspaceUser" },
       "description": "Maps users to workspaces and defines their roles."
    },
    "/posts/{postId}": {
      "schema": { "$ref": "#/entities/Post" },
      "description": "Contains all social media posts. Security rules enforce workspace-level access."
    },
    "/socialAccounts/{accountId}": {
      "schema": { "$ref": "#/entities/SocialAccount" },
      "description": "Stores connected social media account information (non-sensitive)."
    },
    "/notifications/{notificationId}": {
      "schema": { "$ref": "#/entities/Notification" },
      "description": "Stores system notifications for workspaces."
    }
  }
}
